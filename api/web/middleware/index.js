
const auth = require('./authentication');
const swagger = require('./swagger');
const good = require('./good');
const localization = require("./localization");

module.exports = { good, swagger, auth, localization };